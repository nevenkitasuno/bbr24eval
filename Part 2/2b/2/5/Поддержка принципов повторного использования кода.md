В языке C# поддерживаются следующие из этих принципов:

1 за счёт обобщённых типов;
2 за счёт поддержки разных инструментов для инкапсуляции (например класс, namespace);
3 за счёт NuGet-пакетов;
4 насколько я понимаю также за счёт обобщённых типов;
5 за счёт поддержки переиспользования кода путём композиции и наследования;

Список примеров для каждого принципа не исчерпывающий
