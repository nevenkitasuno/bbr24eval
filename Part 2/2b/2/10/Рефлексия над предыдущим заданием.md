- Сделал методы абстрактными т.к. думал что их нужно реализовывать отдельно для каждого конкретного типа
- Сериализацию отдельно от print нужно было сделать тоже, в json или другой формат
- Нужно было смелее использовать рефлексию
- Не вижу в примерах операцию "проверка типа (является ли тип текущего объекта указанным типом)", видимо встроенного в язык достаточно
